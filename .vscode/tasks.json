{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Make build native",
      "command": "make",
      "type": "shell",
      "args": ["-f", "sdl.mk"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "presentation": {
        "reveal": "always"
      },
      "group": "build"
    }
  ]
}
